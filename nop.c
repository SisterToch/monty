#include "monty.h"
/**
 * nop - this function is supposed to
 * do nothing.
 * @stack: points to the head of the stack.
 * @line_number: the line number or instruction number.
 * return: returns nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}

